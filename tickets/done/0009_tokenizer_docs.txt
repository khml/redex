チケット番号: 0009
タイトル: トークナイザ設計・使用ドキュメント作成

概要:
  - `lib/redex/tokenizer.rb` の設計説明と使用例をドキュメント化する
  - トークン種別、各種振る舞い（数値、識別子、キーワード、演算子、括弧、未知文字）の説明
  - 使用方法（API: `Redex::Tokenizer.tokenize` の引数/返り値仕様）と例
  - 制限事項（整数のみ、浮動小数点未対応等）と今後の拡張案

成果物:
  - `tickets/0009_tokenizer_docs.txt`（このファイル）
  - README などに貼る短い使用例（必要なら `docs/` 配下ファイル）

受け入れ基準:
  - `Redex::Tokenizer.tokenize` の使い方が明確に記載されている
  - トークンの種類と意味が一覧で示されている
  - 最低1つの使用例（入力→トークン列）が掲載されている

作業メモ:
  - ドキュメントは日本語で記載
  - 必要に応じて `docs/` に移動し、参照リンクを `README.md` に追加

実行計画:
  概要: トークナイザのエラー方針を確定し、ドキュメント・実装・テストを整合させる。

  ステップ:
    1. 未知文字の扱いを決定する（例外を投げる `Redex::TokenizerError` にするか、`:unknown` を仕様とするか）。
    2. 決定に基づき本ドキュメントを更新して `docs/tokenizer.md` を作成する。
    3. 必要なら `lib/redex/tokenizer_error.rb`（または `lib/redex/errors.rb`）を追加して例外クラスを実装する。
    4. `lib/redex/tokenizer.rb` を仕様に合わせて調整する（未知文字の扱い・トークン種別の明確化）。
    5. `spec/tokenizer_spec.rb` に未知文字・境界条件（空入力・非ASCII・連続演算子など）のテストを追加する。
    6. ドキュメント・テスト・実装をレビューし、チケットをクローズする。

  成果物:
    - `docs/tokenizer.md`（API、トークン一覧、使用例、拡張案）
    - 必要に応じた例外実装ファイル（`lib/redex/tokenizer_error.rb` 等）
    - 追加テスト（`spec/tokenizer_error_spec.rb` / `spec/tokenizer_spec.rb` の拡張）

  注意点:
    - 既存のパーサが `:unknown` を前提にしている場合、例外導入は互換性リスクを伴う。
    - 国際化（非ASCII識別子）や浮動小数点など将来拡張を見越したトークン設計を文書化しておく。
